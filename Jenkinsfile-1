 pipeline {
     agent any

     stages {
         stage('Build') {
             steps {
                 echo "Build number is ${currentBuild.number} was executing ${currentBuild.durationString}"
                 sh 'echo Hello World'
                 sh 'ls -a'
             }
         }
     }
 }